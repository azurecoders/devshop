apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "devshop.fullname" (dict "name" "products" "context" .) }}
  labels:
    {{- include "devshop.labels" . | nindent 4}}
    app.kubernetes.io/name: products
spec:
  replicas: {{ default 1 .Values.products.replicaCount | int }}
  selector:
    matchLabels:
      {{- include "devshop.selectorLabels" (dict "name" "products" "context" .) | nindent 6}}
  template:
    metadata:
      labels:
        {{- include "devshop.selectorLabels" (dict "name" "products" "context" .) | nindent 8}}
    spec:
      containers:
      - name: products
        image: {{ include "devshop.image" (dict "service" .Values.products "image" .Values.image) }}
        imagePullPolicy: {{ .Values.products.image.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.products.service.targetPort | int }}
          name: http
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
          - secretRef:
              name: {{ .Release.Name }}-secret
        resources:
            {{- toYaml .Values.products.resources | nindent 12 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.products.service.targetPort | int }}
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.products.service.targetPort | int }}
          initialDelaySeconds: 5
          periodSeconds: 5
